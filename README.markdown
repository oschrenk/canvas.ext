# README #

Extensions fot the HTML5 Canvas API

- `roundedRect (x, y, width, height, radiusTopLeft, radiusTopRight, radiusBottomRight, radiusBottomLeft, fill, stroke)` draws a rounded rectangle where you can specify the radius of each corner individually (defaults to zero), `fill` defaults to false, `stroke` to true